# Additional requirements for PDF to CSV conversion feature
# Install these packages for full PDF processing capabilities

# Primary PDF processing libraries
tabula-py==2.9.0        # Best for structured tables, requires Java
camelot-py[cv]==0.10.1   # Advanced table extraction with computer vision
pdfplumber==0.11.0       # Lightweight PDF text and table extraction

# Supporting dependencies
PyPDF2==3.0.1           # PDF manipulation
pdf2image==1.17.0       # PDF to image conversion (for camelot)
opencv-python==4.8.1.78 # Computer vision support (for camelot)

# Java requirement note:
# tabula-py requires Java Runtime Environment (JRE)
# Install Java 8+ on your system before using tabula-py

# Installation commands:
# pip install tabula-py camelot-py[cv] pdfplumber PyPDF2 pdf2image opencv-python

# For Ubuntu/Debian systems, also install system dependencies:
# sudo apt-get install default-jre
# sudo apt-get install ghostscript
# sudo apt-get install python3-tk